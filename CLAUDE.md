# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## What this project is

**demo3.0-UI** — the same app as `../demo2.0` (Envoy: dispatches AI-driven outbound calls via Vapi/Twilio/Claude to chase overdue invoices, with a Supabase Storage invoice-ingestion + scheduling queue), wearing the visual system of the sibling product `../../EnvoyIn` (dashboard shell with a collapsible dark sidebar, brand-red `#E31E24`, DM Sans + Bricolage Grotesque, hand-rolled component library). Data + files live in Supabase (Postgres + Storage); the Google Drive/Sheets integration that demo2.0 uses was replaced here (see Changelog).

This was a **UI-only reskin + structural rebuild**, not a feature change: the backend is byte-for-byte identical to demo2.0 (see below), and the goal was full functional parity. If demo2.0 gains a new feature, this project will not pick it up automatically — it's a separate copy, not a shared package.

## Working rules

Same convention as demo2.0: for any non-trivial task, plan first (opus sub-agent / Plan agent), get it reviewed, then execute. Ask clarifying questions rather than guessing on ambiguous points. After any commit that gets pushed, add an entry to the Changelog section below (date, what changed, why) as part of that same push.

## Relationship to demo2.0 and EnvoyIn

- **demo2.0** (`../demo2.0`) — source of truth for all functionality/business logic. This project's `src/lib/*.ts`, `src/app/api/**`, `prisma/schema.prisma`, and `scripts/scheduler.ts` were ported from there with zero logic changes. If a bug is fixed or a feature added in demo2.0's backend, it needs to be manually re-ported here — there's no shared package linking the two.
- **EnvoyIn** (`../../EnvoyIn`) — source of the visual design system (Tailwind theme, `globals.css` utility classes, sidebar shell pattern, component organization), and — as of the 2026-07-22 merge-readiness work — the source of the auth/multi-tenancy/ticket patterns this project now mirrors so it can eventually be merged INTO EnvoyIn. **This project now HAS auth + multi-tenancy** (NextAuth v5, `ownerId` = lowercased email on every table) — the old "no auth, matching demo2.0" statement is superseded (see Changelog 2026-07-22).

## First-time setup

```bash
npm install
cp .env.example .env   # fill in DATABASE_URL + DIRECT_URL (Supabase) and the API keys — see below
npm run db:push        # syncs the Prisma schema to Supabase Postgres
npm run dev:all         # web server (:3010) + scheduler worker together
```

For Vapi webhooks locally: `npm run tunnel` (ngrok on :3010), set `PUBLIC_URL` in `.env` to match.

## Commands

```
npm run dev       # Next.js dev server on :3010 (not :3000 — runs alongside demo2.0)
npm run scheduler # Standalone scheduler worker (node-cron via tsx)
npm run dev:all    # dev server + scheduler worker together
npm run build      # prisma generate + next build
npm run db:push    # Push Prisma schema to Supabase Postgres (add --force-reset to rebuild clean)
npm run tunnel      # ngrok http 3010
```

## Environment / database

- **Supabase Postgres** (migrated off local SQLite on 2026-07-21 — see Changelog). Two URLs in `.env`:
  - `DATABASE_URL` — pooled connection (pgbouncer, port 6543, `?pgbouncer=true`) used at runtime.
  - `DIRECT_URL` — direct connection (port 5432) used by `prisma db push` / any DDL.
- The schema is **Prisma-owned** (`prisma db push`, no migrations dir). `--force-reset` drops & rebuilds all tables clean.
- Data is now in a **shared remote DB**, so the old SQLite file-per-app isolation is gone: if demo2.0 (still SQLite) and demo3.0 ever point at the same Supabase project they'd collide. Keep them on separate projects/schemas if both dispatch.
- `.env` holds Vapi/Twilio/Gemini credentials (same as demo2.0) plus the Supabase keys (`DATABASE_URL`, `DIRECT_URL`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_INVOICE_BUCKET`). No Google credentials — the Drive/Sheets integration was removed. `PUBLIC_URL` (the ngrok tunnel Vapi posts webhooks to) can only point at one app's port at a time, so only one of demo2.0 / demo3.0-UI should be actively dispatching real calls unless you set up a second tunnel.
- **Files:** invoice PDFs live in the Supabase Storage bucket `invoices` (private; accessed server-side via `SUPABASE_SERVICE_ROLE_KEY`, proxied through `/api/files/*`). **Contacts** are `customer` rows (the old "Business Contact Details" sheet folded in), managed at `/app/contacts`. Helpers: `src/lib/storage.ts`, `src/lib/contacts.ts`.
- `.env` is gitignored and has never been committed — keep it that way (it contains live secrets).

## Architecture

### Backend — call flow/scheduling logic still matches demo2.0 (read that project's CLAUDE.md for the full narrative: call flow, scheduling internals, Google Drive integration, the AI brain in `lib/vapi.ts`). Summary: Vapi (voice orchestration) → Twilio (PSTN) → Claude (LLM brain via Anthropic, configured in the Vapi assistant). `src/lib/dispatcher.ts` holds all grouping/business-hours/concurrency/dispatch logic shared by the HTTP routes and the standalone `scripts/scheduler.ts` worker.

**Persistence diverged from demo2.0 in the Supabase migration** (2026-07-21). The data model is now the normalized CRM ERD (`prisma/schema.prisma`, source ERD in `docs/database-erd.md`):
- `Customer` — the debtor (replaces demo2.0's denormalized `contactBusiness`/`abn`/`groupKey` identity). Resolved find-or-create by ABN → fuzzy name via `resolveCustomerId()` in `dispatcher.ts`.
- `Invoice` — ERD invoice columns **plus** the operational scheduling columns (`chaseAfter`, `status`, `attempts`, `groupKey`, caller config) so it still doubles as the queue.
- `InvoiceLineItem` — line items are rows now, not a JSON string; `serializeLineItems()`/`parseLineItemRows()` in `dispatcher.ts` convert at the API boundary so the parser/prompt/UI still speak the old `{description,quantity,unitPrice,amount}` JSON string.
- `Call` — ERD call columns + denormalized lead-invoice snapshot cols (amountDue/currency/banking/invoiceNumber) for the detail view + SMS. `transcript` is native `jsonb`. Linked to invoices via `CallInvoice` (join table; replaces the old `Invoice.callId`).
- `Settings` — singleton, not part of the ERD (added for the scheduler).
- `Payment`, `Note`, `SalesPerson`, `Location` — ERD tables defined for completeness; not written by the app yet.

Prisma **field names were kept identical to demo2.0** (camelCase) and `@map`'d to the ERD's snake_case columns, so the API JSON contracts and UI were left unchanged. Pragmatic type notes: money is `Float` (double, not `numeric`), dates (`invoiceDate`/`dueDate`) are `text` to preserve string-compare logic.

### UI — real routes + shared component library (the structural change from demo2.0)

demo2.0's entire UI lived in one ~4,000-line `src/app/page.tsx` with in-memory screen-switching (no routing). This project restructured that into:

```
src/app/
  app/
    layout.tsx              — ToastProvider + BulkIntakeProvider + AppShellChrome (sidebar shell)
    AppShellChrome.tsx       — collapsible dark sidebar, nav = Dashboard/New Call/Invoices/Queue/Contacts/Settings
    SchedulerStatusPill.tsx  — sidebar footer status (no auth here, so no user block — shows scheduler on/off instead)
    dashboard/page.tsx        — call history + stats (old Home)
    calls/new/page.tsx         — manual call brief (old Compose)
    calls/new/invoice/page.tsx  — single-invoice upload → dispatch (old InvoiceCompose, standalone path)
    calls/live/[id]/page.tsx    — live call view, dark full-bleed while active (old Live)
    invoices/select/page.tsx    — Supabase Storage / upload picker → queue or bulk-dispatch (old SelectInvoiceScreen)
    contacts/page.tsx            — manage contacts (customer rows): view/add/edit phone numbers
    invoices/bulk/page.tsx       — legacy immediate bulk dispatch (old BulkInvoiceScreen)
    queue/page.tsx                — scheduling queue, grouped by debtor (old QueueScreen)
    settings/page.tsx              — scheduler/business-hours/retry config (old SettingsScreen)
src/components/shared/        — Icons, Button, Card, Badge, Toast, ConfirmDialog, Skeleton, Drawer, Modal,
                                 Spinner, WaveformViz, Logo, Hairline, plus two feature-shared pieces:
                                   - InvoiceComposeForm.tsx  — the parsed-invoice review/edit form, reused by
                                     the standalone new-invoice page, the bulk-item edit drawer, and the
                                     queue-invoice edit drawer
                                   - CallDetailDrawer.tsx    — call detail, reused by Dashboard, Bulk, and Queue
                                   - BulkIntakeContext.tsx   — the in-flight bulk-upload/parse/dispatch state,
                                     lifted into a context (mounted in app/layout.tsx) so it survives navigation
                                     between /app/invoices/select and /app/invoices/bulk, since those are now
                                     two separate routes instead of one shared parent component's state
src/lib/
  format.ts, client-types.ts  — formatting helpers and frontend types, ported from demo2.0's page.tsx
  prisma.ts, vapi.ts, dispatcher.ts, sms.ts, nameUtils.ts  — ported from demo2.0
  storage.ts    — Supabase Storage (invoice PDFs); contacts.ts — contacts backed by the customer table
                  (these replace demo2.0's drive.ts)
```

The old call-history "Detail" screen and the bulk-item/queue-invoice "edit" overlays are now `Drawer` overlays (slide-in from the right) instead of full screen transitions, matching EnvoyIn's `TicketDetail` drawer pattern — inspect-in-place rather than navigate-away.

### Design tokens

`src/app/globals.css` — EnvoyIn's `@layer components` classes (`.card`, `.btn-primary/secondary/ghost/danger`, `.input`, `.label`, `.badge`, `.pill`), `.app-bg`/`.sidebar-bg` backgrounds, brand-red Tailwind extension (`tailwind.config.ts`). Demo2.0's own animations with no EnvoyIn equivalent (`wave-bar`, `dot-pulse`, `pulse-ring`, `fade-up`/`fade-in`) were kept and recolored to brand red — they're load-bearing for the Live screen. Demo2.0's cream/burgundy palette, Fraunces/Geist fonts, and `.grain` texture were dropped entirely.

## Changelog

Append-only log — one entry per push: date, what changed, why.

- **2026-07-15** — Initial build. Scaffolded the project, ported the backend verbatim from demo2.0, built the EnvoyIn-style design tokens and shared UI primitive library, built the sidebar dashboard shell, and rebuilt all 9 screens as real routes (parallelized across 5 sub-agents against shared component contracts). Verified with a clean `npm run build`, a full route smoke test, and a live round-trip against the fresh `envoy-demo3.db` (`GET/PUT /api/settings`, `GET /api/calls`, `GET /api/invoices`, `POST /api/scheduler/tick`). Not yet done: visual QA in a browser against EnvoyIn's actual dashboard, and a real end-to-end Vapi dial-out test.
- **2026-07-21** — Replaced the **Google Drive/Sheets** file+contacts integration with **Supabase**. Invoice PDFs now live in a private Supabase Storage bucket (`invoices`) via `src/lib/storage.ts` + `/api/files/*` (list/download-proxy/upload); the "Business Contact Details" sheet was folded into the `customer` table via `src/lib/contacts.ts` + `/api/contacts` (+ `/api/contacts/[id]`), with a new `/app/contacts` screen to view/add/edit contacts and a Contacts sidebar nav item. Added `Invoice.sourceFilePath` linking each queued invoice to its stored PDF; the upload flow now saves local PDFs to the bucket too. A one-time migration moved the existing 15 Drive PDFs + 2 sheet contacts across, then `src/lib/drive.ts`, `/api/drive/*`, the `googleapis`/`xlsx` deps, and all `GOOGLE_*` env were removed. Added `@supabase/supabase-js`. Verified end-to-end: file list/download, parse→queue with `source_file_path` + customer/line-item rows, contact add/edit, and phone lookup all round-trip against Supabase; `npm run build` clean.
- **2026-07-21** — Migrated persistence from local SQLite to **Supabase Postgres** onto the normalized CRM ERD (`docs/database-erd.md`): rewrote `prisma/schema.prisma` (9 ERD tables + `Settings` + operational columns), added `resolveCustomerId`/`serializeLineItems`/`parseLineItemRows` to `dispatcher.ts`, and reworked the API routes to the new tables (line items → `InvoiceLineItem` rows, `Invoice.callId` → `CallInvoice` join, `transcript` → `jsonb`). Prisma field names kept camelCase and `@map`'d to snake_case columns, so the API contracts + UI are unchanged. `.env` switched to pooled `DATABASE_URL` + `DIRECT_URL`; schema pushed with `prisma db push --force-reset`. Verified against live Supabase: queue enqueue creates customer/invoice/line-item rows, settings, dedup, and PATCH all round-trip; `npm run build` clean. Still pending: a real end-to-end Vapi dial-out test.

- **2026-07-22** — **Merge-readiness Phase 1: multi-tenancy + auth + tickets + customer_id** (branch `feature/phase1-multitenancy-auth`, pushed to remote `voiceagent1` — github.com/Shreyank-isoft/voiceagent1, commit `2359f36`). First phase of evolving this app to adopt EnvoyIn's patterns so it can later be merged INTO EnvoyIn (plan: `~/.claude/plans/i-am-working-on-modular-tiger.md`). This is the foundation that unblocks Phases 2–3.
  - **Schema** (`prisma/schema.prisma`, pushed with `--force-reset` — prior data was disposable test data, JSON backup taken first): added `ownerId` (= lowercased email, EnvoyIn's tenant key) to every aggregate root (Customer/Invoice/Call/Payment/Note/SalesPerson/Location); added an EnvoyIn-shaped **`Ticket`** model (one per outbound Call, tagged `outbound`, with a real `customerId` FK EnvoyIn's tickets lack) + a **`Tenant`** config table (EnvoyIn `app_config` analogue: profile blob, per-business `phoneNumber`, members, forecast history, credentials); re-keyed **`Settings`** from the global `id:"singleton"` row to one row per `ownerId`; added NextAuth `User`/`Account`/`VerificationToken` (JWT strategy).
  - **Auth** (mirrors EnvoyIn, adapted to TS + Prisma): NextAuth v5 (Google + magic link) — `src/auth.ts`, `src/auth.config.ts` (edge-safe), `src/middleware.ts` (gates `/app`,`/onboarding`,`/api`; PUBLIC_ROUTES = webhook + cron + `/api/auth`), `src/lib/auth-adapter.ts` (forces `user.id = lowercased email`), `src/lib/access.ts` (`resolveAccess`/`hasRole`/`forbidden`/`unauthorized` + `trim*` stubs), `src/lib/tenant.ts`, `src/lib/members.ts` (Phase-1 stub), `src/lib/email.ts`/`email-provider.ts` (Resend; falls back to logging the sign-in link to the server console when `RESEND_API_KEY` is unset), `/login` + `/verify-email` (Safe-Links inert-confirm) pages. New env in `.env.example`: `AUTH_SECRET`, `GOOGLE_CLIENT_ID/SECRET`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `PUBLIC_BASE_URL`, `CRON_SECRET`. Installed `next-auth@5.0.0-beta.31` + `resend`.
  - **ownerId threading:** every `src/lib/*` data fn and **every** API route now takes/derives `ownerId` first and scopes by it; single-row reads filter by `id AND ownerId` (IDOR guard); role gates applied (agent+ to dispatch/queue/edit, admin+ to settings). `src/lib/tickets.ts` added (EnvoyIn-shaped, ownerId-first). Storage keys owner-prefixed (`<ownerId>/…`) with an IDOR guard on download.
  - **Ticket-per-call:** both dispatch paths (`/api/calls/dispatch`, `dispatcher.dispatchInvoiceGroup`) create an `outbound` Ticket; the **webhook** (`/api/calls/webhook`) resolves tenant from `call.ownerId` (with a `?owner=` fallback stamped onto the Vapi `server.url`), updates the linked ticket, and backfills the Customer (`backfillCustomerFromCall`). New `/api/tickets` + `/api/tickets/[id]`.
  - **On-demand scheduler:** `runSchedulerTick(ownerId)` + new `runAllTenantsTick()`; new secured `POST /api/cron/dispatch` (Bearer `CRON_SECRET`) for serverless/external schedulers; `scripts/scheduler.ts` now calls `runAllTenantsTick` (local dev only).
  - **Verified:** `npm run build` clean (25 routes + edge middleware); a data-layer isolation script passed **14/14** owner-scoping/IDOR checks; live route smoke passed (unauth `/api/*`→401, `/app/*`→`/login`, cron guard 401/200, `/api/auth/providers` 200).
  - **NOT yet tested (do before relying on it):** (1) full **authenticated end-to-end** — sign in (Google needs OAuth creds; magic link works via the console-logged link when Resend is unset), dispatch a real call, confirm the `outbound` Ticket is created and flipped to Resolved by the Vapi webhook. No live Vapi dial-out was run. (2) The magic-link **email** path with a real Resend key. (3) Multi-tenant dispatch with >1 real tenant. (4) Google OAuth redirect/`AUTH_SECRET` in any deployed env. Per-tenant phone number + credential consolidation are deferred to Phase 3G (dispatch still uses the shared `TWILIO_PHONE_NUMBER` env for now). [UPDATE: the magic-link sign-in flow (console-link mock) + owner-scoped data access were subsequently verified E2E during Phase 2 — see below. Still untested: real Vapi dial-out, real Resend email, Google OAuth, >1-tenant dispatch.]

- **2026-07-22** — **Merge-readiness Phase 2: Customers CRM + Outbound dashboard + Onboarding** (same branch `feature/phase1-multitenancy-auth`, pushed to remote `voiceagent1` — github.com/Shreyank-isoft/voiceagent1, commit `c6f1b3b`). Builds on Phase 1's tenancy/tickets foundation.
  - **Customers master + detail** (absorbs the old Contacts screen): `src/lib/customers.ts` (owner-scoped `getCustomers` with open-invoice/outstanding aggregates, `getCustomer` detail with nested invoices/tickets/calls, create/update with IDOR guard); `/api/customers` + `/api/customers/[id]`; `/app/customers` master table (search, add-customer drawer with delivery address) and `/app/customers/[id]` detail (header + Invoices / Tickets (both channels) / Calls / Payments sub-tabs; Payments is a Phase-3 stub). Nav: `Contacts`→`Customers`, old `/app/contacts` route now redirects to `/app/customers` (new `IconChart` added for the Outbound tab).
  - **Outbound dashboard** (`/app/outbound`, new sidebar tab): server-side aggregation `src/lib/outbound-stats.ts` + `/api/outbound/stats` (call-outcome breakdown, resolution rate, queue depth, outbound-ticket status, 14-day calls-per-day series). Hand-rolled SVG chart primitives in `src/components/shared/charts/Charts.tsx` (StatTile / DonutChart / VBar) following EnvoyIn's no-charting-library convention — built via the **dataviz skill**: form-first (stat tiles for KPIs, donut for status mix, vertical bars for time), a **validated status palette** (resolved `#10b981` / voicemail `#94a3b8` / failed `#E31E24` / active `#f59e0b` — CVD ΔE and normal-vision checks pass; each slice always carries a legend + numeric label), 2px segment gaps, hover layers.
  - **Onboarding** (`/onboarding` + `src/components/onboarding/OnboardingWizard.tsx`, framer-motion 5-step wizard: Business identity + delivery address → seed outbound Contacts → Call-moment defaults (voice/manner/objective + business hours) → Credentials placeholder → Review). `/api/onboarding` creates the Tenant row, seeds Settings business-hours, and seeds Customer rows. **New-owner gate**: `src/app/app/layout.tsx` (now an async server component) redirects owners with no Tenant row to `/onboarding`; `/onboarding` bounces already-configured owners back to `/app` — mirrors EnvoyIn's page-level onboarding check.
  - **Verified (authed E2E, via the magic-link console-link mock):** sign-in establishes a session (`session.user.id` = lowercased email); a new owner is redirected to `/onboarding`; completing onboarding creates the tenant + seeds 2 customers and unlocks `/app` (dashboard 200); `/api/customers` returns exactly the seeded owner-scoped rows; `/api/outbound/stats`, `/api/tickets?channel=outbound`, `/app/customers`, `/app/outbound` all 200; `/app/contacts`→`/app/customers` redirect works. `npm run build` clean (30 routes). Smoke-test tenant data was cleaned up afterward.
  - **Not built yet:** Phase 3 (payments consolidation, deterministic invoice parser + Gemini fallback, forecasting from live data, analytics inbound/outbound segmentation, Team & Access field-hiding, on-demand cron wiring docs, per-tenant credentials + phone number).

- **2026-07-22** — **Merge-readiness Phase 3 (partial): Team & Access, per-tenant credentials, payments, cron wiring, forecasting** (same branch `feature/phase1-multitenancy-auth`). Builds on Phases 1–2. **No schema changes** — everything reuses existing columns (`Tenant.members`/`credentials`/`phoneNumber`, `Payment`, `Ticket.payment`), so **no `db push`/migration is needed**. `npm run build` clean (37 routes).
  - **3C — Team & Access (field-hiding + real members):** `src/lib/members.ts` now backs a real member list on `Tenant.members` jsonb — `findMembershipByEmail` (Postgres jsonb `@>` containment), `listMembers`/`addMember`/`updateMember`/`removeMember`/`acceptInviteIfPending`. `src/lib/access.ts` `trimCustomerForAccess`/`trimInvoiceForAccess` (+ new `trimCallForAccess`) are now real: viewer/agent responses have banking (`bankName`/`bsb`/`accountNumber`/`swiftCode`/remittance) and customer PII (`contactPhone`/`email`/`abn`/`creditLimit`) **deleted server-side before serialisation** (enforced in `/api/customers`, `/api/customers/[id]`, `/api/invoices`, `/api/invoices/[id]`; calls routes expose no banking). New `/api/me` (role + `canSeeSensitive` for UI gating), `/api/members` (GET/POST, admin+), `/api/members/[id]` (PATCH/DELETE, admin+). New **Team & access** card in Settings (`TeamSection.tsx`) — invite by email, role dropdown (admin/agent/viewer), remove; self-hides for non-admins.
  - **3G — Per-tenant credentials + caller-id:** `src/lib/credentials.ts` — `resolveDispatchConfig(ownerId)` merges each tenant's own Vapi/Twilio/Anthropic keys (from `Tenant.credentials`) + caller-id (`Tenant.phoneNumber`) over the process env, so dispatch is per-tenant with an env fallback (single-tenant local dev still works with just `.env`). Secrets are **AES-256-GCM encrypted at rest** when `CREDENTIALS_SECRET` is set (else `plain:`-prefixed with a logged warning — dev only); the browser only ever gets a masked `••••1234` + presence flags. Both dispatch paths (`dispatcher.dispatchInvoiceGroup`, `/api/calls/dispatch`) and `vapi.dispatchVapiCall` now use the resolved config instead of raw `process.env`. New `/api/credentials` (GET status / PUT save, **owner-only**) + **Outbound credentials & caller-id** card in Settings (`CredentialsSection.tsx`).
  - **3F — Payments consolidation:** `src/lib/payments.ts` merges demo3.0's AR received-payments (`Payment` rows) with EnvoyIn-style inbound payments (`Ticket.payment` jsonb) into one owner-scoped ledger + summary (received/credits/outstanding); `recordPayment` writes a `Payment` row (IDOR-guarded), advances `Invoice.paidAmount`, and flips the invoice to `resolved` once fully paid. New `/api/payments` (GET ledger+summary, optional `?customerId=`; POST record, agent+). New global **Payments** tab (`/app/payments`, new `IconWallet` nav item) with summary tiles + ledger + a record-payment drawer; the Customer-detail **Payments** sub-tab is now live (was a stub).
  - **3A — On-demand scheduler wiring:** `runAllTenantsTick` + secured `POST /api/cron/dispatch` already existed (Phase 1); added `vercel.json` (Vercel Cron every minute) + `docs/scheduler-cron.md` documenting both drivers (local always-on worker vs external cron → the guarded endpoint) and the "only one driver at a time" caveat.
  - **3D — Forecasting from live data:** `src/lib/forecasting.ts` — pure-arithmetic decomposition (linear trend + day-of-week seasonality + AR(1) residual carry) over a **live** daily series (outbound `Ticket` activity, last 42 days, zero-filled), projecting 14 days with an ~80% band; plus cash context (upcoming invoice due within horizon, recent recorded collections). New `/api/forecasting` + `/app/forecasting` page (new `IconTrend` nav item) with an SVG line/area **ForecastChart** (history solid + forecast dashed + confidence band + hover crosshair) built per the **dataviz skill** (single series ⇒ no legend, title names it; recessive grid; 2px non-scaling strokes).
  - **NOT yet tested (do before relying on it):** (1) **Field-hiding** end-to-end with a real second signed-in member — the logic + build are verified, but a live "sign in as an agent/viewer, confirm banking/PII absent from `/api/*` payloads" pass hasn't been run. (2) **Per-tenant credentials at real dispatch** — the resolve/encrypt path is unit-level sound but no real Vapi call with a tenant-specific key was placed. (3) **Forecast/payments visuals** in a browser against real data volume. (4) Still outstanding from Phase 1: real Vapi dial-out, real Resend email, Google OAuth, >1-tenant dispatch. See `docs/phase3-testing.md` for the exact local test steps.

- **2026-07-22** — **Merge-readiness Phase 3 completion: invoice-template parser (3B) + analytics segmentation (3E)** (same branch `feature/phase1-multitenancy-auth`). Completes Phase 3. `npm run build` clean (39 routes); `npx tsc --noEmit` clean.
  - **3B — Deterministic invoice parser + Gemini fallback:** installed `pdf-parse`; new `src/lib/invoice-parse.ts` (shared `ParsedInvoiceSchema` + `normaliseParsedInvoice`, extracted from the route so both paths share one contract) and `src/lib/invoice-templates.ts` (a registry keyed by vendor fingerprint with regex/positional extractors → the same `ParsedInvoice` shape; `parseDate`/`parseAmount` helpers; `matchTemplate` + `isExtractionValid`). Refactored `/api/calls/parse-document`: extract the PDF text layer (pdf-parse), `matchTemplate` → if a known vendor matches **and** the extraction has the essentials (number + amount + a date), return it directly with **no Gemini call** (and **no `GEMINI_API_KEY` needed**); otherwise fall back to the existing Gemini vision path unchanged (or a clear 422 if no key). Seeded **5 real vendor templates from the user's sample PDFs**: Spiced Tea Chai (Xero), Quest Software International, Altus Financial, Green Design Indoor Plant Hire, Vertel/Vertical Telecoms. Added `src/types/pdf-parse.d.ts`. **Tested:** `scripts/test-invoice-templates.ts` asserts number/dates/total/ABN/BSB/account/line-items against text fixtures for all 5 → **5/5 pass**; `scripts/parse-invoice-pdf.ts` runs the real pdf-parse→match pipeline on an actual PDF path for end-to-end confirmation.
  - **3E — Analytics inbound/outbound segmentation:** `src/lib/analytics.ts` segments tickets by the `tags` jsonb (`outbound`/`inbound`, SLA/category tags ignored) into per-channel status mix + resolution rate + a 14-day per-channel series; `/api/analytics`; `/app/analytics` page (new `IconPie` nav item) built via the **dataviz skill** — StatTiles, a channel-mix DonutChart, an outbound-status DonutChart, and **small-multiple** VBars (one per channel, never a dual-series bar); categorical channel palette red/blue/gray always paired with the donut legend + counts. Inbound stays at zero (with an explanatory note) until EnvoyIn's inbound tickets share the workspace post-merge.
  - **Phase 3 is now COMPLETE** (3A–3G all done). Remaining untested items are unchanged from the previous entry (all require live third-party creds): field-hiding with a real 2nd member, per-tenant creds at real Vapi dispatch, real Vapi dial-out / Resend / Google OAuth / >1-tenant dispatch. See `docs/phase3-testing.md`.

- **2026-07-24** — **Outbound Tickets screen + DB-backed per-customer invoices + dev-login** (same branch `feature/phase1-multitenancy-auth`, committed locally, NOT pushed). Closes the two remaining requirements-doc UI gaps and makes the app fully clickable end-to-end against the linked Supabase. No schema migration (new statuses are plain strings). `npx tsc --noEmit` clean; clean `npm run build` (exit 0). **Note:** run the prod build with the dev server STOPPED — a running dev server locks the Prisma engine DLL on Windows (`EPERM`).
  - **Outbound Tickets screen** (`/app/tickets`, new sidebar tab, `IconTicket`): envoyin-style kanban (lanes In progress / Needs follow-up / Resolved) + a Board/Table toggle + search, and a slide-in `src/components/shared/TicketDetailDrawer.tsx` (linked customer + invoice(s), AI summary, tags, notes, mark-resolved, and "View full call" reusing `CallDetailDrawer`). `src/lib/tickets.ts` enriched `getAllTickets`/`getTicket` with call+customer joins and added `deriveOutboundTicketStatus` (Queued/Calling/Voicemail/Failed/Resolved, derived from the linked Call since `Ticket.status` only holds Incoming/In Progress/Resolved). `TicketStatusBadge` in `Badge.tsx`. **Gotcha:** outbound tickets are `channel:"phone"` with an `outbound` TAG (not channel), so the list API + page filter by `?tag=outbound`; `GET /api/tickets` returns the enriched shape + `derivedStatus`, and `/api/tickets/[id]` gained a `PATCH` (resolve / add note).
  - **Every invoice persisted + browsed per-customer (no separate library):** new `src/lib/invoices.ts` `createInvoiceRow`/`persistInvoice` (the single insert path: customer-resolve + dedup + groupKey + line items), and `src/lib/invoice-ingest.ts` (the parse core — pdf-parse → template → Gemini — extracted from `/api/calls/parse-document` so the route AND the import script parse identically). `POST /api/invoices` now takes a `status` (default **`stored`**, browsable/dispatchable-later; bulk "Queue" flow passes `status:"pending"` explicitly to keep enqueue behaviour). The single-invoice upload page persists a `stored` invoice on parse. Customer detail Invoices sub-tab now lists **all** invoices with a **PDF link** (`/api/files/invoice?path=`) + status badge + **Queue / Call now** actions (`getCustomer` selects `sourceFilePath`/`toNumber`/`groupKey`). `stored` added to `InvoiceStatusBadge` + the `Invoice.status` schema comment. Dispatch still reads purely from DB columns — zero re-parse.
  - **Bulk ingest:** `scripts/import-invoices.ts` (npm `import:invoices`) loads a directory of PDFs OR a JSON file into Supabase as `stored` rows (idempotent via dedup). Ran it on the 15 sample PDFs → 15 stored invoices across 2 debtor customers. `scripts/seed-demo.ts` seeds a Tenant row + customer phones + representative calls/tickets across every lane. `scripts/pw-smoke.mjs` is a local Playwright smoke harness (magic-link console sign-in + screenshots every screen).
  - **Dev-login bypass** (mirrors EnvoyIn): `src/auth.config.ts` registers a `Credentials` provider `id:"dev-login"` only when `!process.env.VERCEL && ALLOW_DEV_LOGIN==="1"` (returns `{id:email,email}` for any valid email → same ownerId path), `+ trustHost:true`; `/login` shows a gated "DEV ONLY" form. Enabled via `ALLOW_DEV_LOGIN=1` in `.env` (documented in `.env.example`). Lets you sign in as any email locally without Resend/Google.
  - **Fix:** added `src/app/app/page.tsx` redirecting `/app` → `/app/dashboard` (sign-in previously 404'd because there is no `/app` index route).
  - **Tested this session:** `tsc`/build clean; Playwright smoke **0 console/API errors** across Tickets (board/table/drawer), Customers → detail invoices (PDF links + actions), Outbound, Analytics, Forecasting, Queue; dev-login verified (seeded owner → app, new email → onboarding). **Still needs live creds:** real Vapi dial-out + webhook resolution, Resend/Google sign-in, >1-tenant dispatch, field-hiding with a real 2nd member. The 15 sample-invoice PDFs live in `sample invoices/` on disk but are intentionally NOT committed (business PDFs).

- **2026-07-24** — **Browser-test fixes: outstanding = unpaid + past-due, multi-currency, reconcile, onboarding/sidebar/invoices polish** (same branch `feature/phase1-multitenancy-auth`, NOT yet committed). Driven by a Playwright test pass of the app; two product decisions were resolved by the user (outstanding semantics; keep multi-currency test data as-is and handle it in code). No schema changes. `npx tsc --noEmit` clean; clean `npm run build` (exit 0, all routes). All changes re-verified live in the browser via Playwright MCP (0 console/API errors on every edited screen). **Reminder unchanged:** run the prod build with the dev server STOPPED (running dev server locks the Prisma engine DLL on Windows → `EPERM`).
  - **Outstanding redefined = unpaid + past-due, per currency (decision):** new `src/lib/money.ts` — `MoneyByCurrency` bucket type, `isOutstanding` (not cancelled, `paidAmount < amountDue`, and past its `dueDate` — missing/unparseable due date treated as owed), `sumOutstandingByCurrency`, `sumAmountsByCurrency`, `mergeMoney`, `totalMoneyMagnitude`, `fmtMoneyByCurrency`. "Paid" is derived from **`paidAmount`**, NOT `status:'resolved'` (the call webhook sets `resolved` on collection-resolution regardless of payment). `customers.ts` (`getCustomers`/`getCustomer`) and `payments.ts` (`getPaymentSummary`) now compute outstanding in code (text due dates + multi-currency can't be aggregated in SQL); `CustomerSummary.outstanding`/`PaymentSummary.outstanding` changed `number` → `MoneyByCurrency`. Dropped the `OPEN_INVOICE_STATUSES` consts. Effect: past-due `stored` invoices now count (ACME went from $577k queued-only → **$1.16M + USD 4,459.88**).
  - **Multi-currency handled explicitly (decision):** `fmtAmount` now uses `minimumFractionDigits: 2` (fixes dropped trailing zeros like `$578,999.7`); non-AUD keeps its explicit code prefix (`USD 4,459.88`). Totals are **never summed across currencies** — customer list/detail + Payments outstanding tiles render `fmtMoneyByCurrency` (e.g. `$… + USD …`); the Queue group total and `CallDetailDrawer` aggregate use `sumAmountsByCurrency` instead of a single mislabeled `$` sum.
  - **Reconcile / Mark paid:** new **Mark paid** action on each open invoice row in the customer-detail Invoices sub-tab — POSTs the remaining balance to the existing `/api/payments` (reuses `recordPayment`, which advances `paidAmount` and flips the invoice to `resolved`), so it drops out of Outstanding. Verified: paying the USD invoice removed the `USD` bucket from the customer + Payments totals.
  - **Chart date bucketing fix:** added `localDay()` to `format.ts` and used it in `outbound-stats.ts` + `analytics.ts` for both the window keys and the row keys (they previously mixed local `setHours(0,0,0,0)` windows with UTC `toISOString().slice(0,10)` keys, so today's activity fell out of the buckets → empty bars). `forecasting.ts` left as-is (internally all-UTC, self-consistent).
  - **Onboarding fixes** (`OnboardingWizard.tsx`): the stage-5 "back to step 1" bounce was Next's client router-cache serving the stale "no-tenant → /onboarding" gate result — the finish handler now does a **hard nav** (`window.location.assign('/app/dashboard')`) so the `/app` layout gate re-evaluates on the server with the just-created Tenant row. Step-1 **Back → Exit** (signs out via `signOut({callbackUrl:'/login'})`, the only coherent exit for an owner with no tenant). Final button renamed **"Finish setup" → "Finish & go to dashboard"**.
  - **Sidebar/chrome** (`AppShellChrome.tsx`): the `<nav>` is now `flex-1 min-h-0 overflow-y-auto` (scrolls when tall; logo + footer stay pinned); added a **Sign out** button in the footer (`signOut → /login`, new `IconLogout`); **removed the "New Call" nav tab** (route + dashboard "New call" button kept).
  - **Invoices screen** (`invoices/select/page.tsx`): reworked the narrow single-column list into a full-width **desktop table** (checkbox / Invoice file / Size / Uploaded, header select-all, selected-row highlight, `overflow-x-auto`) for both the Supabase and Upload tabs; all `useBulkIntake` wiring (queue/dispatch) preserved — presentation-only.
  - **A11y + small UX:** `Drawer` close button `aria-label="Close"`; Settings business-day toggles expose `aria-pressed`; Queue shows a neutral "No invoice ready to dispatch in this group." hint when Dispatch is disabled for a non-obvious reason; Tickets board cards show the linked invoice # (from the title) + a "No summary yet" fallback; the Outbound resolution-rate caption changed "of calls that reached a person" → "of completed calls" to match the terminal-call denominator.
  - **Dropped as non-bugs:** the ticket note "timestamp collision" (real `ml-2` margin; only the a11y snapshot flattened the whitespace) and the "duplicate `/api/settings` fetch" (dev-only React StrictMode double-invoke). The intermittent `/api/settings` 500 wasn't reproducible → left as monitor-only. Per the user, the demo data (USD invoice, ~$577k amounts, all-April due dates, "iSoft" as both tenant and a customer) is intentional and was NOT re-seeded.
  - **Test data touched (dev DB):** a throwaway tenant `qa-onboarding-test@example.com` from onboarding verification; ACME invoice #282183 (USD) marked paid; one ticket resolved during the earlier test pass. Re-run `seed-demo` for a clean slate.

- **2026-07-24** — **Envoyin-parity redesign: unified dashboard, pre-login landing + globe, full customer detail, invoice drawer** (same branch `feature/phase1-multitenancy-auth`, committed locally? NO — NOT committed/pushed). Visual/UX pass to match the sibling `envoyin` app and add a marketing site. **No schema migration** (all fields already existed). `npx tsc --noEmit` clean; clean `npm run build` (Compiled successfully); Playwright QA of landing/dashboard/customer/invoice = 0 console errors (bar a harmless favicon 404). Dev server left running on :3010 (`dev:all`).
  - **Unified dashboard** (`src/app/app/dashboard/page.tsx`) — rebuilt to mirror envoyin: search + status/outcome/date filters, gradient KPI cards (Total/Queued/Requires-attention/Resolved) incl. a cursor-tracking `TiltCard`, the Call-outcomes donut + Calls-per-day bar folded in from Outbound, the outbound kanban, and a red-gradient **Live Activity** rail (recent calls, pulsing dot). 15s silent auto-refresh. New shared `src/components/shared/TicketBoard.tsx` (lanes **Queued / Requires attention / Resolved**). **Retired** the standalone **Tickets** + **Outbound** nav items (`AppShellChrome.tsx`); `src/app/app/tickets/page.tsx` and `.../outbound/page.tsx` are now `redirect('/app/dashboard')` stubs. `/api/outbound/stats` kept as the dashboard chart source.
  - **Animation parity** — added `.gradient-shine`, `.btn-gradient`, `.tilt-card`/`.tilt-shine`, `.metric-card-interactive`, `.metric-card-fade` to `globals.css` (ported from envoyin), plus the full landing CSS block (hero atmosphere/orbs/grain, `.reveal*` scroll-reveal, marquee, `.globe-pin*`) with a `prefers-reduced-motion` guard.
  - **Pre-login marketing landing** (`src/app/page.tsx` now renders `src/components/landing/Landing.tsx`; was a redirect) — full outbound story from the Envoy pitch handoff (`Claude design presentation handoff.zip`): sticky nav, hero + **rotating canvas globe** (ported `src/components/landing/Globe.tsx` + `globeLandPoints.ts` from envoyin, AU office pins, drag-to-spin), marquee, count-up problem stats (AU$76B / 78 hrs / 42
- **2026-07-24** — **Envoyin-parity redesign: unified dashboard, pre-login landing + globe, full customer detail, invoice drawer** (same branch `feature/phase1-multitenancy-auth`; NOT committed/pushed). Visual/UX pass to match the sibling `envoyin` app and add a marketing site. **No schema migration** (all fields already existed). `npx tsc --noEmit` clean; clean `npm run build` (Compiled successfully); Playwright QA of landing/dashboard/customer/invoice = 0 console errors (bar a harmless favicon 404). Dev server left running on :3010 (`dev:all`).
  - **Unified dashboard** (`src/app/app/dashboard/page.tsx`) — rebuilt to mirror envoyin: search + status/outcome/date filters, gradient KPI cards (Total/Queued/Requires-attention/Resolved) incl. a cursor-tracking `TiltCard`, the Call-outcomes donut + Calls-per-day bar folded in from Outbound, the outbound kanban, and a red-gradient **Live Activity** rail (recent calls, pulsing dot). 15s silent auto-refresh. New shared `src/components/shared/TicketBoard.tsx` (lanes **Queued / Requires attention / Resolved**). **Retired** the standalone **Tickets** + **Outbound** nav items (`AppShellChrome.tsx`); `src/app/app/tickets/page.tsx` and `.../outbound/page.tsx` are now `redirect('/app/dashboard')` stubs. `/api/outbound/stats` kept as the dashboard chart source.
  - **Animation parity** — added `.gradient-shine`, `.btn-gradient`, `.tilt-card`/`.tilt-shine`, `.metric-card-interactive`, `.metric-card-fade` to `globals.css` (ported from envoyin), plus the full landing CSS block (hero atmosphere/orbs/grain, `.reveal*` scroll-reveal, marquee, `.globe-pin*`) with a `prefers-reduced-motion` guard.
  - **Pre-login marketing landing** (`src/app/page.tsx` now renders `src/components/landing/Landing.tsx`; was a redirect) — full outbound story from the Envoy pitch handoff (`Claude design presentation handoff.zip`): sticky nav, hero + **rotating canvas globe** (ported `src/components/landing/Globe.tsx` + `globeLandPoints.ts` from envoyin, AU office pins, drag-to-spin), marquee, count-up problem stats (AU$76B / 78 hrs / 42%), how-it-works, an **animated live-board mock**, a scripted transcript demo, use cases, CTA, iSOFT footer. Uses envoyin tokens (red `#E31E24`, DM Sans/Bricolage/**Instrument Serif** added to the font `<link>`), NOT the handoff's Gellix/maroon. `/` is public (outside the middleware matcher); "Launch app" -> `/app/dashboard`. **Fixed** an SSR hydration mismatch by seeding the live-board mock deterministically (randomise only client-side).
  - **Customer detail = full accounts master** (`customers/[id]/page.tsx`) — new **Details** sub-tab (Identity/Contact/Address/Commercial) surfacing every column, and an edit form covering all of them with **Sales person / Location** dropdowns (new owner-scoped `GET /api/salespersons` + `/api/locations`). `src/lib/customers.ts` extended (`CustomerSummary` + `salesPerson`/`location` relations + `listSalesPersons`/`listLocations`); `/api/customers[/id]` zod schemas widened. New `scripts/seed-crm-details.ts` (npm `seed:crm`) seeds SalesPerson (Anisha/Shammi) + Location (WENTYSHOP/DEEWHYSHOP) rows and fills accountCode/creditLimit/terms/flags/balance. Ran it for `shreyank.sinha@isoftanz.com.au` (2 customers).
  - **Invoice depth** — enriched the customer Invoices sub-tab rows (Invoice date / Total / Paid / Balance) and made each open a new slide-in `src/components/shared/InvoiceDetailDrawer.tsx` (amounts, customer contact block, line-items table, bank/remittance, payment history) matching the Bill-Settlement screenshot. Backed by a new `GET /api/invoices/[id]` (invoice + lineItems + payments + customer, access-trimmed for viewer/agent).
  - **NOT done:** commit/push (ask first). Prod build must run with the dev server STOPPED (Windows Prisma DLL lock).
